<div class="search__filters__section {#do_search_cmp_filters_period #}">
    <h3 class="search__filters__title">{_ Creator _}</h3>

    <ul class="{#do_search_cmp_filters_creator#}">
    	{# <li><input name="{{ id.title }}" id="{{ id.title|slugify }}" type="checkbox" value="{{ id.id }}"><label for="{{ id.title|slugify }}">{{ id.title }}</label></li> #}
    </ul>
</div>
