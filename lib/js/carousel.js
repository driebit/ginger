
$.widget( "ui.slider", {

    options: {
    },

    _create: function() {

        // var me = this,
        //     widgetElement = $(me.element),

        //     carouselConfig = widgetElement.data('carousel-config'),
        //     obj = jQuery.parseJSON(carouselConfig);

        // if (carouselConfig){
        //     $(me).slick(obj);
        // } else {
        //     $(me).slick();
        // }
    }
});

