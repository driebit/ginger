Usage:

    {% include "_map.tpl" id=[ID] container="UNIQUE_ID" height=HEIGHT %}

for a single resource and:

	{% include "_map.tpl" id=COLLECTION container="UNIQUE_ID" height=HEIGHT %}

for an array of resources. 

The height parameter [int] is optional and defaults to 100%.
